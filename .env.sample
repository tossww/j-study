# J-Study Environment Variables
# Copy this to .env.local and fill in your values

# Anthropic API Key (for flashcard generation)
ANTHROPIC_API_KEY=sk-ant-xxxxx

# Vercel Postgres (auto-populated by Vercel when you link a database)
POSTGRES_URL=
POSTGRES_PRISMA_URL=
POSTGRES_URL_NON_POOLING=
POSTGRES_USER=
POSTGRES_HOST=
POSTGRES_PASSWORD=
POSTGRES_DATABASE=

# Vercel Blob (auto-populated by Vercel when you add Blob storage)
BLOB_READ_WRITE_TOKEN=
